# requirements.txt — Python 3.12 compatible (Kaggle)
# Replaces: gym==0.17.3, mujoco-py==2.0.2.13, numpy==1.19.2
 
gymnasium[mujoco]>=0.29.1   # replaces gym + mujoco-py; auto-installs MuJoCo binaries
numpy>=1.24.0                # 1.19.2 does not build on Python 3.12
torch>=2.1.0                 # already available on Kaggle GPU kernels
tensorboard>=2.13.0          # used by logger.py (SummaryWriter)
psutil>=5.9.0                # used by logger.py (RAM check)
tqdm>=4.65.0                 # used by main.py
opencv-python>=4.7.0         # used by play.py (cv2)
 
